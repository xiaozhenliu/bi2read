# BiMyScribe 路线图

状态：当前交付状态的单一事实源  
最近核验：2026-08-19
已发布基线：v0.3.0（以 `docs/releases/v0.3.0.md` 为证据）
当前内部版本：0.4.0-dev.1（以 `Cargo.toml` 与 `Cargo.lock` 为证据）

## 文档定位

本路线图说明 BiMyScribe 从当前产品走向可信视频消费工具的阶段路径，只回答四个问题：

1. 当前已经可靠交付什么；
2. 距离下一阶段还缺什么；
3. 各阶段按什么顺序推进；
4. 用什么结果判断一个阶段已经完成。

长期产品行为与验收口径由 PRD 的需求 ID 定义，用户流程与界面进入设计规范，模块契约进入
架构或 ADR，样本、实验过程和实施证据进入对应 issue，已发布事实进入版本化 Release Notes。
其他文档不得复制本文件的交付状态和优先级。

状态词只有以下四种：

- **已交付**：实现、验证、错误行为和用户说明均有证据；
- **当前**：正在跨越的最高优先级产品门槛；
- **候选**：方向已经明确，但必须等待前一阶段证据或排期裁决；
- **阻塞**：缺少明确输入、授权或外部证据，不能安全推进。

## 产品方向

BiMyScribe 将从“能够生成本地转写文件”演进为“能够快速、可信地消费长视频内容的桌面
产品”。演进顺序是：先建立可靠且可追溯的多语言 Evidence，再生成忠实可读正文，随后提供
带来源引用的章节与摘要，最后形成图文精读和内置消费体验。

可信基础能力未达到阶段门槛前，不用下游摘要、提示词或界面包装掩盖上游质量问题。

## 当前基线

v0.3.0 已可靠交付以下基础：

- **输入与执行**：Bilibili URL、BV、AV、短链、多分 P、持久队列、取消、重试和恢复；
- **本地 Evidence**：FFmpeg 标准化、FunASR Runtime、带时间戳和说话人的原始 JSON 与
  Markdown；
- **派生文档**：确定性合并相邻说话人片段、可选本地 LLM 整理、失败回退和最终 `full.md`；
- **产品入口**：macOS Slint 桌面界面，以及共享同一生产流水线的 CLI；
- **Runtime**：bundled/external 解析，以及 native-uv、Docker Compose backend；
- **交付与治理**：版本化 Release Notes、公开快照过滤、顺序化发布门禁和分阶段设计流程。

这些事实证明产品流水线和回退接口存在，但不证明真实视频的识别质量已经满足稳定阅读要求。
当前最重要的产品限制是：用户无法清楚选择适合视频语言的 Runtime，并核对任务实际使用的
Runtime、模型和语言配置。

## 路线阶段

| 目标版本 | 用户获得的结果 | 状态 | 完成门槛 | 对应需求 |
| --- | --- | --- | --- | --- |
| v0.3.0 可运行的本地流水线 | 用户可以提交 Bilibili 视频并获得可追溯的本地转写文件 | 已交付 | 输入、队列、Runtime、Evidence、回退和 GUI/CLI 均有发布证据 | F0 基线 |
| v0.4.0 可信的中英文转写 | 用户或 Agent 为中文、英文视频选择 Runtime 和语言，获得可核对的时间与说话人 Evidence | 已交付 | App 与 CLI 的冻结选择、v2 contract、既定 `paraformer-en` 英文 Docker/App 回归、中文契约结构检查、speaker/时间 Evidence 和失败行为均有证据；用户选择不被静默改变 | ASR-06—ASR-08、ASR-11—ASR-12、CLI-08 |
| v0.5.0 忠实可读正文 | 用户得到比原始转写更易读、又不隐藏删改的正文 | 候选 | 正确语言模型基线已完成；校正保留逐片段来源映射；删除、幻觉和失败回退有明确约束 | TXT-07、AI-02、AI-10 |
| v0.6.0 可核对的快速理解 | 用户可以阅读章节、摘要和重点，并跳回对应原视频 | 候选 | 忠实正文达到质量门槛；生成结论具有时间引用；增强失败不影响 Evidence 和正文 | AI-03—AI-09 |
| v0.7.0 图文精读 | 用户可以结合正文、关键帧、幻灯片和 OCR 消费视频 | 候选 | 文本 Evidence 稳定；视觉资产契约明确；图片与文本时间轴可核对地对齐 | VIS-01—VIS-09、READ-03 |

表中未发布版本是规划目标，不提前把 `Cargo.toml` 改成对应的正式产品版本。目标发布线进入
本地实现验证后，按仓库的内部版本纪律使用 `-dev.N` 内部版本；目标能力完成并验证后，仍须按照
发布纪律切换正式版本、同步 Changelog 和版本化 Release Notes，才转为“已交付”。若范围或
优先级变化，先在本路线图调整目标版本，不把尚未交付的规划写成发布事实。

## 最近完成：v0.4.0 可信的中英文转写

固定英文样本的起始基线已经完成，并证明旧 Runtime 曾用中文 ASR 模型处理英文音频。当前
Runtime v2 按既定方案使用中文/英文 Paraformer profile，SenseVoiceSmall 只负责 auto 检测；
英文 Docker/App 回归已按该既定 profile 完成，中文只完成契约、结构和失败行为验证，不设置正确率
benchmark。

本阶段的完成定义：

1. 用户能查看每个 Runtime 的来源、模型、建议语言和已知限制，并自行选择；产品不认证其
   兼容性，也不静默代替用户切换；
2. 既有英文视频能用明确选择的 Runtime 和语言完整跑通 Docker/App 生产路径；中文主路径
   通过 contract、结构、时间/speaker 字段和失败行为验证；
3. 英文 speaker 归属、时间映射、原始 Evidence 和失败行为可与既有 raw transcript 参考核对；
4. 既有英文 raw transcript 只作为回归参考；本版本不伪造人工 gold 的 WER/CER，也不把中文
   正确率 benchmark 作为门槛；
5. UI、CLI 和用户文档能显示 Runtime 说明、用户选择和实际处理身份；CLI 提供稳定 JSON，
   不发生交互式阻塞或静默回退；
6. 短视频、混合语言和低置信度回退按实际能力标记，但不作为本阶段的阻塞边界案例。

起始基线不得自动重复执行。样本标识、哈希、耗时、指标明细和后续方案的比较结果由对应
issue 保存，本文件只在证据改变阶段状态、优先级或完成判断时更新。

## 已知差距

| 优先级 | 差距 | 产品影响 | 归属阶段 |
| ---: | --- | --- | --- |
| 1 | Runtime 选择及实际语言、模型配置不透明 | 用户可能误用不匹配的 Runtime，且难以判断结果是否可信 | v0.4.0 |
| 2 | LLM 校正缺少逐片段保真和删减约束 | 文本可能更流畅，但也可能丢失原始信息 | v0.5.0 |
| 3 | 章节、摘要和重点尚无稳定的来源引用 | 用户无法核对生成结论，不能建立可信消费体验 | v0.6.0 |
| 4 | 视觉资产及其时间对齐契约尚未完成 | 关键帧、幻灯片和 OCR 无法可靠进入图文精读 | v0.7.0 |

## 最近完成

- **v0.3.0 产品基线**：本地流水线、Evidence、派生文档、GUI/CLI 和 Runtime 回退已形成
  可发布基础；
- **固定英文起始基线**：已在 10 分钟门槛内得到可重复比较的 WER/CER 报告；
- **优先级裁决**：已确认当前首要问题是语言与 ASR 模型不匹配，v0.4.0 因此先于上下文
  校正、摘要和图文能力。

## 维护规则

- 新证据改变优先级、阶段门槛或完成状态时，只更新本文件；PRD 不保存开发快照；
- 每个“当前”或“候选”项进入实施前必须有独立 issue，记录输入、范围、checklist 和证据；
- 路线图只保留足以解释决策的证据结论，不保存样本哈希、逐次实验数据或实现过程；
- 版本发布后，把用户可见事实写入对应 Release Notes，不把本文件复制成日期进度报告；
- 无法从源码、测试、产物或人工验收证明的结论标为阻塞或未知，不以代码入口存在代替交付
  证据。
